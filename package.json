{
  "name": "repocop-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for RepoCop repository and change review tools",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "benchmark": "node scripts/run-benchmarks.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.29.0",
    "dotenv": "17.4.1",
    "express": "5.2.1",
    "zod": "4.3.6"
  }
}
